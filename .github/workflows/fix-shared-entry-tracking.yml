name: Fix shared entry tracking
on:
  push:
    branches: [main]
    paths:
      - "scripts/fix-shared-entry-tracking.mjs"
      - ".github/workflows/fix-shared-entry-tracking.yml"
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: node scripts/fix-shared-entry-tracking.mjs
      - run: |
          git config user.name "KvizToGo"
          git config user.email "actions@users.noreply.github.com"
          git add scroll-tracking.js
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Broji ulaze preko podijeljenih pitanja"
          git push
