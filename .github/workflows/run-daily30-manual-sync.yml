name: Apply Daily30 manual sync
on:
  push:
    paths:
      - 'patch_daily30_manual_sync.py'
      - '.github/workflows/run-daily30-manual-sync.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python patch_daily30_manual_sync.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add daily30.js
          git commit -m "Force Daily30 sync on profile actions" || exit 0
          git push
