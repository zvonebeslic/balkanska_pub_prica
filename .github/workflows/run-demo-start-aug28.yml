name: Run demo leaderboard Aug 28 reset
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-demo-start-aug28.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 patch_demo_start_aug28.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add leaderboards.js
          git diff --cached --quiet || git commit -m "Start demo leaderboard history on Aug 28 2026"
          git push
