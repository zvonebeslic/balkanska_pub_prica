name: Run Daily30 how patch
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-daily30-how.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 patch_daily30_how.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add online-kviz.html
          git diff --cached --quiet || git commit -m "Add Daily 30 explanation"
          git push
