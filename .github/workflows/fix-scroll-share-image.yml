name: Fix scroll share image
on:
  push:
    branches: [main]
    paths:
      - "scripts/fix-scroll-share-image.mjs"
      - ".github/workflows/fix-scroll-share-image.yml"
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: node scripts/fix-scroll-share-image.mjs
      - run: |
          git config user.name "KvizToGo"
          git config user.email "actions@users.noreply.github.com"
          git add scroll-znanje.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Dijeli scroll pitanje kao sliku i kratki link"
          git push
