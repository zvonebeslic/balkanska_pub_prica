import fs from 'node:fs';
function rep(t,a,b,label){if(!t.includes(a))throw new Error('missing '+label);return t.replace(a,b)}
let p=fs.readFileSync('profil.html','utf8');
p=rep(p,'${historyMode ? "" : `<div class="my-quiz-actions"><button class="btn btn--soft edit-quiz-btn" data-id="${q.id}">✏️ Uredi</button><button class="btn btn--danger cancel-quiz-btn" data-id="${q.id}">${q.status === "cancelled" ? "Ponovno objavi" : "Otkaži"}</button></div>`}','${historyMode ? "" : `<div class="my-quiz-actions"><button class="btn btn--soft edit-quiz-btn" data-id="${q.id}">✏️ ${currentLang === "en" ? "Edit" : "Uredi"}</button><button class="btn btn--danger cancel-quiz-btn" data-id="${q.id}">${q.status === "cancelled" ? (currentLang === "en" ? "Republish" : "Ponovno objavi") : (currentLang === "en" ? "Cancel" : "Otkaži")}</button></div>`}','profile action buttons');
fs.writeFileSync('profil.html',p);

let m=fs.readFileSync('moderation-messages.js','utf8');
m=rep(m,'  let fallbackClient = null;','  let fallbackClient = null;\n\n  function uiLang() { return document.documentElement.lang === "en" || localStorage.getItem("kviztogo_lang") === "en" ? "en" : "hr"; }\n  function tx(hr, en) { return uiLang() === "en" ? en : hr; }','moderation helper');
m=rep(m,'if (label) label.textContent = "Ime uklonjeno";\n    if (panelName) panelName.textContent = "Ime uklonjeno";','if (label) label.textContent = tx("Ime uklonjeno", "Name removed");\n    if (panelName) panelName.textContent = tx("Ime uklonjeno", "Name removed");','restricted');
m=rep(m,'bubble.setAttribute("aria-label", "Poruke");','bubble.setAttribute("aria-label", tx("Poruke", "Messages"));','bubble label');
m=rep(m,"backdrop.innerHTML = '<section class=\"modmsg-card\" role=\"dialog\" aria-modal=\"true\" aria-label=\"Poruke\"><div class=\"modmsg-head\"><div class=\"modmsg-title\">Poruke</div><button class=\"modmsg-close\" type=\"button\" aria-label=\"Zatvori\">×</button></div><div id=\"modmsg-list\"></div></section>';","backdrop.innerHTML = `<section class=\"modmsg-card\" role=\"dialog\" aria-modal=\"true\" aria-label=\"${tx('Poruke','Messages')}\"><div class=\"modmsg-head\"><div class=\"modmsg-title\">${tx('Poruke','Messages')}</div><button class=\"modmsg-close\" type=\"button\" aria-label=\"${tx('Zatvori','Close')}\">×</button></div><div id=\"modmsg-list\"></div></section>`;",'modal');
m=rep(m,'bubble.setAttribute("aria-label", n ? `Poruke, ${n} nepročitanih` : "Poruke");','bubble.setAttribute("aria-label", n ? (uiLang() === "en" ? `Messages, ${n} unread` : `Poruke, ${n} nepročitanih`) : tx("Poruke", "Messages"));','unread label');
m=rep(m,'return new Intl.DateTimeFormat("hr-HR", { day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit" })','return new Intl.DateTimeFormat(uiLang() === "en" ? "en-GB" : "hr-HR", { day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit" })','date locale');
m=rep(m,'note.textContent = "Ime mora imati od 2 do 20 znakova.";','note.textContent = tx("Ime mora imati od 2 do 20 znakova.", "The name must be between 2 and 20 characters.");','name length');
m=rep(m,'note.textContent = "To ime je zauzeto ili ga nije moguće spremiti. Odaberi drugo ime.";','note.textContent = tx("To ime je zauzeto ili ga nije moguće spremiti. Odaberi drugo ime.", "That name is taken or cannot be saved. Choose another name.");','name taken');
m=rep(m,'note.textContent = "Novo ime je spremljeno. Ponovno se prikazuješ na ljestvici.";','note.textContent = tx("Novo ime je spremljeno. Ponovno se prikazuješ na ljestvici.", "Your new name has been saved. You are visible on the leaderboard again.");','name saved');
m=rep(m,'note.textContent = "Promjena imena trenutačno nije uspjela.";','note.textContent = tx("Promjena imena trenutačno nije uspjela.", "The name change could not be completed right now.");','name failed');
m=rep(m,"list.innerHTML = '<div class=\"modmsg-empty\">Učitavam poruke...</div>';","list.innerHTML = `<div class=\"modmsg-empty\">${tx('Učitavam poruke...','Loading messages...')}</div>`;",'loading');
m=rep(m,"if (!identity) { list.innerHTML = '<div class=\"modmsg-empty\">Nema poruka.</div>'; return; }","if (!identity) { list.innerHTML = `<div class=\"modmsg-empty\">${tx('Nema poruka.','No messages.')}</div>`; return; }",'no identity');
m=rep(m,"if (!messages.length) { list.innerHTML = '<div class=\"modmsg-empty\">Nema poruka.</div>'; setUnreadCount(0); return; }","if (!messages.length) { list.innerHTML = `<div class=\"modmsg-empty\">${tx('Nema poruka.','No messages.')}</div>`; setUnreadCount(0); return; }",'no messages');
m=rep(m,'action.textContent = "Odaberi novo ime";','action.textContent = tx("Odaberi novo ime", "Choose a new name");','choose name');
m=rep(m,"rename.innerHTML = '<div class=\"modmsg-rename-row\"><input class=\"modmsg-rename-input\" maxlength=\"20\" autocomplete=\"nickname\" placeholder=\"Novo ime\"><button class=\"modmsg-btn primary\" type=\"button\">Spremi</button></div><div class=\"modmsg-note\"></div>';","rename.innerHTML = `<div class=\"modmsg-rename-row\"><input class=\"modmsg-rename-input\" maxlength=\"20\" autocomplete=\"nickname\" placeholder=\"${tx('Novo ime','New name')}\"><button class=\"modmsg-btn primary\" type=\"button\">${tx('Spremi','Save')}</button></div><div class=\"modmsg-note\"></div>`;",'rename form');
m=rep(m,"list.innerHTML = '<div class=\"modmsg-empty\">Poruke trenutačno nisu dostupne.</div>';","list.innerHTML = `<div class=\"modmsg-empty\">${tx('Poruke trenutačno nisu dostupne.','Messages are currently unavailable.')}</div>`;",'unavailable');
fs.writeFileSync('moderation-messages.js',m);
